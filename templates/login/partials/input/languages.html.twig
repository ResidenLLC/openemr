{#
Render the language list, label and selectbox

* Set language_container_class_list with an array of classes to override the default classes
* Set language_label_class_list with an array of classes to override the default classes
* Set language_select_class_list with an array of classes to override the default classes

@package OpenEMR
@subpackage Login
@author Robert Down <robertdown@live.com
@copyright Copyright (c) 2023 Robert Down <robertdown@live.com>
@copyright Copyright (c) 2023 Providence Healthtech
@license https://github.com/openemr/openemr/blob/master/LICENSE GNU General Public License 3
#}
{% if displayLanguage %}
{% block display_languages %}
{# Florin: commented this out because there is a bug in Admin/Config/All Languages Allowed
   - if you uncheck that option, some of the OpenEMR pages break
   - so it's easier just to hide the language drop-down in the UI
   - this was also done for the portal login page 
#}
<!--

<div class="{{ language_container_class_list|default(['form-group', 'row'])|join(' ') }}">
    <label for="language" class="{{ srOnly }} {{ language_label_class_list|default(['col-form-label', 'col-sm-4'])|join(' ') }}">{{ "Language"|xlt }}</label>
    <div class="col">
        <select class="{{ language_select_class_list|default(['form-control'])|join(' ') }}" name="languageChoice" size="1">
            <option value="{{ defaultLangID|attr }}" selected>{{ "Default"|xlt }} - {{ defaultLangName|xlt }}</option>
            {% for l in languageList %}
                <option value="{{ l.lang_id|attr }}">{{ l.trans_lang_description|text }}</option>
            {% endfor %}
        </select>
    </div>
</div>

-->
{% endblock %}
{% endif %}
